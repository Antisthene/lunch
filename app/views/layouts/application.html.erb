<!DOCTYPE html>
<html>
  <head>
    <title>Lunch</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %> 
      <div class="flex items-center">
      <%= button_to "Home", "/", method: :get, class: "btn py-2 ml-2 bg-green-300" %> 
      <%= button_to "Selection", selections_path, method: :get, class: "btn py-2 ml-2 bg-blue-300" %> 
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn py-2 ml-2 bg-red-300" %> 
      </div>
    <% else %>
      <%= button_to "Sign in", new_user_session_path, class: "btn py-2 ml-2 bg-blue-300" %> 
    <% end %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block" id="alert"><%= alert %></p>
    <main class="container mx-auto mt-28 px-5">
      <%= yield %>
    </main>
  </body>
</html>
